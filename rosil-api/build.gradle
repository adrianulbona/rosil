mainClassName = 'ro.utcn.kdd.rosil.api.RosilApi'

dependencies {
    compile project(':rosil-predict')
    compile 'com.sparkjava:spark-core:2.2'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
